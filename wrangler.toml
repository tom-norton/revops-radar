name = "revops-radar"
pages_build_output_dir = "docs"

# Cross-device Hide/Applied sync (functions/api/state.js). Once this file is
# present with pages_build_output_dir set, it is the source of truth for
# bindings on this Cloudflare Pages project -- the dashboard's Bindings tab
# becomes read-only. This sidesteps a known-unreliable dashboard binding UI.
[[kv_namespaces]]
binding = "STATE_KV"
id = "2a041aa827eb4f16b43d8289510b6cc2"
