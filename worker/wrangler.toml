# Deploy config for the relay. Deployed by .github/workflows/worker-deploy.yml on any push
# to main that touches this directory, so merging is what ships a change here.
#
# This file exists mainly for the cron triggers below. Everything else about the Worker
# was set up by hand in the dashboard, but a schedule is not a setting -- it is the thing
# that decides when the scan runs, so it belongs in the repo next to the code that reads
# it, where a change to one is reviewable against the other.
name = "revops-relay"
main = "telegram-relay.js"
compatibility_date = "2026-09-04"

# GH_TOKEN and TELEGRAM_SECRET are deliberately absent, and are not part of a deploy:
# secrets attach to the Worker by name and survive every upload. They are set once, in the
# Cloudflare dashboard under the Worker's Settings -> Variables, and never in a tracked file.

[triggers]
# These are UTC, like every cron. They are not the schedule -- scanDueAt() is, reading
# America/New_York at firing time. These only have to be a superset: every UTC minute that
# could possibly be 8:00, 12:30 or 20:00 (weekdays) or 9:00 (weekends) Eastern, under
# either offset. EDT (UTC-4) matches the first of each pair, EST (UTC-5) the second, and
# scanDueAt() discards whichever does not match on the day -- including the day-of-week,
# which it reads locally, so 8pm Friday landing on Saturday in UTC is not a problem. That
# is what makes the clocks going back on 1 Nov 2026 a non-event here.
#   8:00  -> 12:00Z / 13:00Z     12:30 -> 16:30Z / 17:30Z
#   20:00 -> 00:00Z / 01:00Z     9:00  -> 13:00Z / 14:00Z
crons = ["0 0,1,12,13,14 * * *", "30 16,17 * * *"]
